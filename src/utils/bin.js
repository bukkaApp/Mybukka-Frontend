// const subMenuArr = [];
  // for (let a = 0; a < menus.length; a++) {
  //   if (menus[a]._id === item._id) {
  //     const { submenus, options } = lopper(menus[a], item);
  //     if (submenus.length === menus[a].submenus.length) {
  //       if (options.length === menus[a].submenus.options.length) {
  //         subMenuArr.push({
  //           ...menus[a],
  //           quantity: menus[a].quantity + 1,
  //         });
  //       }
  //     }
  //   }
  //   subMenuArr.push(menus[a]);
  // }
  // return subMenuArr;

  // const menufound = menus.filter(menu => menu._id === item._id);
  // if (menufound.length <= 0) return null;
  // const OneMenuFound = menufound.length === 1;
  // if (OneMenuFound && menufound[0].submenus.length === 0 && item.submenus.length === 0) return null;